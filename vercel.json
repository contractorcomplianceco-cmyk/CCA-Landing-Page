{
  "buildCommand": "BASE_PATH=/ PORT=5173 pnpm --filter @workspace/cca-landing build",
  "outputDirectory": "artifacts/cca-landing/dist",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
