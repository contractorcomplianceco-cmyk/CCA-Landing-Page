kind = "web"
previewPath = "/"
title = "Contractor Compliance Authority"
version = "1.0.0"
id = "artifacts/cca-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20059

[services.development]
run = "pnpm --filter @workspace/cca-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cca-landing", "run", "build" ]
publicDir = "artifacts/cca-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20059"
BASE_PATH = "/"
